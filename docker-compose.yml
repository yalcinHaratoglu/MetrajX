services:
  db:
    image: postgres:16-alpine
    container_name: metrajx-db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: metrajx
      POSTGRES_USER: metrajx
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U metrajx -d metrajx"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
